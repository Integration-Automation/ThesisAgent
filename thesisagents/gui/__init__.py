"""PySide6 desktop UI for ThesisAgents.

Lazily imports PySide6 so the rest of the project keeps working when
the ``[gui]`` extra is not installed. Public entry point is
``thesisagents.gui.app.main``.
"""
